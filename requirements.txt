# ============ 深度学习框架 ============
torch>=2.0.0
torchvision>=0.15.0

# ============ 计算机视觉 ============
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
matplotlib>=3.7.0
timm  # SAM-HQ 依赖

# ============ 模型库 ============
transformers>=4.30.0
safetensors
segment-anything  # SAM 官方库（SAM-HQ 依赖）

# ============ 数据处理 ============
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm

# ============ API 服务（可选） ============
fastapi>=0.100.0
uvicorn>=0.23.0
streamlit>=1.25.0

# ============ RAG 相关（后续使用） ============
chromadb
sentence-transformers

# ============ 开发工具 ============
jupyter